apiVersion: apps/v1
kind: Deployment
metadata:
  name: camera-stream-health-api
  namespace: camera-health
  labels:
    app: camera-stream-health-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: camera-stream-health-api
  template:
    metadata:
      labels:
        app: camera-stream-health-api
    spec:
      containers:
        - name: camera-stream-health-api
          image: charansuryakilana/camera-stream-health-api:53783fa4332db9b6e0f52aef145e555aeb236a86
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1
              memory: 1Gi
